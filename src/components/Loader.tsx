// Loader.js

const Loader = () => (
  <div className="flex justify-center items-center h-screen">
    <div className="loader"></div> {/* Simple CSS or SVG loader */}
  </div>
);

export default Loader;